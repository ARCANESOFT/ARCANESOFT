const config = {
    endpoint: '/dashboard/media/api',

    supportedImages: [
        'image/gif',
        'image/jpeg',
        'image/png',
        'image/svg+xml'
    ]
};

export default config;
